{
  "name": "cloudflare-gauth-proxy",
  "private": false,
  "version": "0.1.0",
  "description": "Edge-cached proxy for Google's OIDC discovery document and signing keys, with a stale-KV fallback, deployed as a Cloudflare Worker.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.21.3",
    "@cloudflare/workers-types": "^5.20260722.1",
    "typescript": "^5",
    "vitest": "^4.1.10",
    "wrangler": "4.114.0"
  },
  "overrides": {
    "undici": "7.29.0"
  }
}
